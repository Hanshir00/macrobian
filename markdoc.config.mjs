import { defineMarkdocConfig } from '@astrojs/markdoc/config';

// Standard rich text only (headings, lists, links, bold, etc.).
// Custom inline components could be added here later if wanted.
export default defineMarkdocConfig({});
