---
// Wraps <Tier> children in a responsive grid.
---
<div class="pricing">
  <slot />
</div>

<style>
  .pricing {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.6rem;
    margin: 2.5rem 0;
    align-items: stretch;
  }
</style>
